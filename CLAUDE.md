
We are using `uv`, use it to run tests, add dependencies, run the application, etc

## Quality
Use `sensors check .` regularly to get information about the code and test quality of your changes. If you just need a quick status check without details, you can run this first instead of running tests or other checks directly.
